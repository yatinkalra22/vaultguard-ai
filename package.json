{
  "name": "vaultguard-ai",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:push": "supabase db push"
  },
  "devDependencies": {
    "supabase": "^2.85.0",
    "turbo": "^2.8.20",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@8.15.0",
  "pnpm": {
    "overrides": {
      "path-to-regexp": ">=8.4.0",
      "lodash": ">=4.18.0"
    }
  }
}
